# Gitleaks configuration — extends the default ruleset with allowlists for
# known false positives in this repo.

[extend]
useDefault = true

[[allowlists]]
description = "Planning docs embed test fixtures that assert key-like strings are rejected; they contain marker strings, not key material"
paths = ['''^docs/superpowers/plans/.*''']
