# Project Guidelines

- Use TypeScript with strict mode
- React functional components with hooks only
- Dark industrial UI theme with Tailwind CSS
- Always generate Juniper SRX-compatible JSON output
- Run `npm run build` to verify changes compile
- Test with `npm test` before committing
- Always update the README and TODO before commits
